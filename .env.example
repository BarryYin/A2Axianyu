# 复制为 .env.local 或 .env 后填入真实值。部署时在服务器上同样配置。

# SecondMe OAuth2（必填，在 SecondMe 开放平台申请）
SECONDME_CLIENT_ID=你的client_id
SECONDME_CLIENT_SECRET=你的client_secret
SECONDME_REDIRECT_URI=https://你的域名/api/auth/callback

# 数据库（必填）。SQLite 路径需可写，例如服务器上用绝对路径
DATABASE_URL=file:./dev.db

# SecondMe API 与 Token 端点（必填）
SECONDME_API_BASE_URL=https://app.mindos.com/gate/lab
SECONDME_OAUTH_URL=https://go.second.me/oauth/
SECONDME_TOKEN_ENDPOINT=https://app.mindos.com/gate/lab/api/oauth/token/code
SECONDME_TOKEN_REFRESH_ENDPOINT=https://app.mindos.com/gate/lab/api/oauth/token/refresh

# 站点公网地址（必填，用于 OAuth 回调与 Agent API 链接）
NEXT_PUBLIC_BASE_URL=https://你的域名

# 可选：Pexels API Key，用于商品图搜索（不填则用 Openverse）
# PEXELS_API_KEY=
